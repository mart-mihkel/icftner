# Continuous Prompt Tuning in Language Models

## Development

Use [uv](https://docs.astral.sh/uv/) for project management and [typst](https://typst.app/) for typestting.

Run `make sync` to setup a local venv, see [Makefile](./Makefile) for other development commands.

## Usage

```bash
uv run cli.py --help
```
